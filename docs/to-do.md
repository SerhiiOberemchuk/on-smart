1. [x] Додати можливість додавати товари і комплекти копіюванням вже існуючих, з подальшим редагуванням.
2. [x] Додати можливість взагалі не відображати товар на сайці, лише в адмінці. Зараз при встановленні кількості 0 - товар залишається на сайті з приміткою IN ARRIVO. Це не змінюємо! Додати чекер "Приховати товар", щоб він не відображався на сайті, але був доступний до редагування в адмінці.
3. [x] Додати можливість видалення комплекту (може вже і є, але я не знайшла).
4. [x] В картці товару та комплекту зробити одну кнопку "Зберегти все", щоб не потрібно було зберігати кожен блок окремо.
5. [x] Можливість змінювати порядок фото, без видалення і повторного додавання.
6. [x] Для вибору супутніх товарів відкривати каталог, вибирати товар зі списку чекерами (одночасне додавання кількох товарів, в т.ч. варіантів).
7. [x] Додати можливість змінювати назви файлів після збереження.
8. [x] Опис товару - Заголовок опису та Характеристики - Назви секції зробити необов'язковими до заповнення.
9. [x] Окреме поле для переліку ключових слів, за яким можна знайти цей товар (але вони можуть бути відсутні в тексті опису та характеристиках)
10. [x] Можливість виділення та копіювання тексту з сайту - не робимо, щоб не погіршувати UX свайперів.
11. [x] Disponibile per l'acquisto для комплектів не відображати, якщо більше 15 шт

12. [ ] Провести аудит функцій на критичні помилки (видалення без where, відсутність транзакцій, часткові оновлення), а також перевірити й виконати оптимізацію запитів для пришвидшення роботи та зменшення навантаження на БД і сервер. Відкладено, повернемося пізніше.

13. [ ] Мігрувати інтеграцію PayPal на новий V6 SDK (`@paypal/react-paypal-js/sdk-v6`: `PayPalProvider` + `PayPalOneTimePaymentButton`/хуки). Зараз використовується legacy API (`PayPalScriptProvider`, `PayPalButtons`, `PayPalMessages`), який досі підтримується у v10 і працює коректно, але з часом буде задепрекейчений. Не терміново — планова робота на майбутнє. Klarna та SumUp залишаємо як є (їхні поточні інтеграції через REST + офіційні віджети є канонічними, кращих бібліотек немає). НЕ поєднувати з фазами клієнтських акаунтів (пп. 14–20).

**Клієнтські акаунти** — специфікація: `docs/specs/client-accounts/` (обов'язкова до прочитання, починати з `00-overview.md`; фази та критерії приймання — `05-rollout.md`). Порядок: 14 → 15 → 16 → 17/18 (паралельно) → 19 → 20.

14. [x] Фаза 1 — Auth-фундамент: `lib/admin-emails.ts`, фікс baseURL `lib/auth-client.ts`, конфіг `lib/auth.ts`, `lib/auth-mail.ts`, `requireCustomerSession()`, `.env.example` (ADMIN_EMAILS). → `docs/specs/client-accounts/05-rollout.md`
15. [x] Фаза 2 — Схеми БД: `customer_profiles`, `user_addresses`, `wishlist_items`, FK на `orders.userId`. `drizzle/0032_account_tables.sql` згенеровано; власник застосував міграцію. → `docs/specs/client-accounts/05-rollout.md`
16. [x] Фаза 3 — Уніфікований auth UI (`/accedi`, `/registrati`, `/verifica-email`, `/password-dimenticata`, `/reimposta-password`), кнопка акаунта в хедері, дії `app/actions/account/auth/*` (роль зі списку ADMIN_EMAILS при реєстрації), cutover: verif ON + видалено стару адмін-авторизацію. **Перед деплоєм:** runtime-тест (реєстрація→лист→вхід; адмін re-verify) + SPF/DKIM assistenza. → `docs/specs/client-accounts/05-rollout.md`
17. [x] Фаза 4 — Кабінет: `/account/ordini` (+деталь з ownership+404), `/account/profilo` (дані + дефолти + зміна пароля), `/account/indirizzi` (CRUD, single-default, ліміт 5), сайдбар, матчер `/account/:path*` у `proxy.ts`. Некешовані Suspense-острівці (§17/§18). Список замовлень порожній до Фази 6. → `docs/specs/client-accounts/05-rollout.md`
18. [x] Фаза 5 — Wishlist: сердечко на картках (клієнтський Zustand-стор `store/wishlist-store.ts`, гість → діалог входу), `/account/preferiti`, дії `app/actions/account/wishlist/*`. Картки лишаються кешованими (стан сердечка — клієнтський). → `docs/specs/client-accounts/05-rollout.md`
19. [x] Фаза 6 — Готово, включно з поліруванням (2026-07-06): `createOrderAction` бере userId із сесії **і жорстко відхиляє гостей** (захист від прямого виклику дії); нова `/checkout/page.tsx` (акаунтний чекаут: підсумок/consegna/fatturazione/pagamento + віджет із props); **інлайн-дозаповнення на чекауті** — міні-форма фактурації (`checkout/components/CheckoutBillingForm.tsx`, hidden-поля зберігають дефолти) + інлайн «+ Nuovo indirizzo» (реюз `AddAddressForm`), чекбокс «Richiedi fattura» на замовлення; лінк «Vedi l'ordine nel tuo account» на completato; блок «Акаунт клієнта» в адмін-деталі замовлення + фільтр `?userId=` у списку. **Потрібен runtime-тест оплати (4 методи) перед деплоєм.** — Оригінальний опис Фази 6 (Новий односторінковий чекаут для залогінених) (`/checkout`, нова index-сторінка): всі дані з акаунта (дефолтні адреса/доставка/оплата/фактура), інлайн-дозаповнення відсутніх даних із збереженням в акаунт, нова дія `create-account-order`, рефактор віджетів оплати на props (поведінка для гостей без змін), CTA на `/carrello` маршрутизує за сесією, блок «Клієнт» в адмінці. Старий wizard для гостей НЕ чіпати. Зафіксувати базлайн конверсії. → `docs/specs/client-accounts/05-rollout.md`
20. [x] Фаза 7 — Cutover у коді: **лише `proxy.ts`** — матчер `+ /checkout/:path*`, гість на `/checkout/*` → `/registrati?redirect=/carrello` (#7), `/checkout/completato*` лишається доступним без сесії. Wizard недосяжний для гостей, код лишається; rollback = прибрати `/checkout` з матчера. Жорстке відхилення `createOrderAction` для гостей — у Фазі 8. **Деплой лише після runtime-тесту й вікна стабільності.** → `docs/specs/client-accounts/05-rollout.md`

23. [~] Funzione di recesso (art. 54-bis Codice del Consumo, D.Lgs. 209/2025, обов'язково з 19.06.2026): **основний флоу — з акаунта** `/account/recesso` (вибір замовлення зі списку з назвами товарів, prefill ім'я/email; пункт «Diritto di recesso» у сайдбарі) + секція на деталі замовлення; публічна `/recesso` — опис процесу + CTA «Recedere dal contratto qui» → акаунт + **згорнутий гостьовий фолбек-модуль** (юридично обов'язковий для замовлень без акаунта, укладених після 19.06.2026 до cutover; можна видалити після спливу їхніх вікон відмови — обережно: ст. 53 може розтягнути вікно до 12 міс.) + лінк у футері на всіх сторінках + оновлено §2 «Resi» на `/garanzia` + email-підтвердження з датою/часом (assistenza) + Telegram + блок «Запити на відмову» в адмін-деталі зі зміною статусу. Кнопка підтвердження — точно «Conferma recesso» (comma 5). Код готовий (build+test green). **Міграції тепер застосовуються автоматично при старті контейнера** (`scripts/migrate.mjs` у CMD Dockerfile; статус у консолі Aruba з префіксом `[migrate]`; `MIGRATE_STRICT=1` — блокувати старт при невдачі; ручний резерв: `GET /api/run-migrations`, тепер admin-only). **Dev-БД: одноразово виконати `DROP TABLE withdrawal_requests;` і `npx drizzle-kit migrate`** (таблиця порожня; перший запуск було перервано — таблиця створилась без FK/індексів, повтори падали на "already exists"). Потрібен runtime-тест (подання з кабінету і гостьове, доставка листа). → `docs/functionality.md` §6-bis

21. [ ] Мігрувати legacy `loading.tsx` на модель «миттєвий каркас + Suspense-скелетони»: `app/(client)/carrello/loading.tsx` та `app/(admin)/admin/dashboard/loading.tsx` — винести DB-залежні частини цих сторінок у малі async server-компоненти під `<Suspense>` зі структурними скелетонами, після чого видалити обидва `loading.tsx`. Відтепер `loading.tsx` заборонено (AGENTS.md "Instant Shell & Streaming", `docs/code-style-rules.md` §18). Окрема задача — не поєднувати з фазами 14–20.

22. [x] Фаза 8 — Wizard видалено (build green; git-тег `pre-phase8-wizard-removal`): видалено кроки `informazioni/consegna/pagamento/riepilogo`, `components/CheckoutPagesComponents/`, `store/checkout-store.ts`; типи винесено в `types/checkout-flow.types.ts`; віджети на **required props** (fallback на стор прибрано); `RepilogoComponent`/`CompletionCleanup` відв'язано від checkout-store; редіректи старих URL → `/checkout`; `functionality.md` §3 переписано. **Незворотне після деплою — потрібен runtime-тест.** — Оригінал: Видалення старого wizard (лише після вікна стабільності Фази 7, git-тег перед видаленням): кроки `informazioni`/`consegna`/`pagamento`/`riepilogo` з компонентами, `store/checkout-store.ts`, гостьовий `createOrderAction`, маршрутизація CTA за сесією; редіректи старих URL кроків на `/checkout`; переписати `docs/functionality.md` §3 під новий чекаут. → `docs/specs/client-accounts/05-rollout.md`

